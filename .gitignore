_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
vendor
.playwright-mcp
# Ad-hoc screenshots dropped at the repo root during UI testing.
# Site images live under assets/, so they are unaffected.
/*.png
